import { isUiExtensionType, uiExtensionTemplates } from '../../constants.js';
export function getUIExtensionTemplates(extensionType) {
    const filteredFlavors = [];
    if (extensionType === 'web_pixel_extension') {
        filteredFlavors.push('react');
    }
    return uiExtensionTemplates.filter((template) => !filteredFlavors.includes(template.value));
}
export function isValidUIExtensionTemplate(extensionType, uiExtensionTemplateValue) {
    return (isUiExtensionType(extensionType) &&
        Boolean(getUIExtensionTemplates(extensionType).find((extensionTemplate) => extensionTemplate.value === uiExtensionTemplateValue)));
}
//# sourceMappingURL=template-configuration.js.map