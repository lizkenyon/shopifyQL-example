import { App, Identifiers } from '../../models/app/app.js';
interface BundleOptions {
    app: App;
    bundlePath: string;
    identifiers: Identifiers;
    bundle: boolean;
}
export declare function bundleUIAndBuildFunctionExtensions(options: BundleOptions): Promise<void>;
export {};
