import { App } from '../models/app/app.js';
import { output } from '@shopify/cli-kit';
export declare type Format = 'json' | 'text';
interface InfoOptions {
    format: Format;
    /** When true the command outputs the env. variables necessary to deploy and run web/ */
    webEnv: boolean;
}
export declare function info(app: App, { format, webEnv }: InfoOptions): Promise<output.Message>;
export declare function infoWeb(app: App, { format }: Omit<InfoOptions, 'webEnv'>): Promise<output.Message>;
export declare function infoApp(app: App, { format }: Omit<InfoOptions, 'webEnv'>): Promise<output.Message>;
export {};
