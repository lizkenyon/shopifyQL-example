import { App, Identifiers } from '../../models/app/app.js';
export interface EnsureDeploymentIdsPresenceOptions {
    app: App;
    token: string;
    appId: string;
    appName: string;
    envIdentifiers: Partial<Identifiers>;
}
export declare function ensureDeploymentIdsPresence(options: EnsureDeploymentIdsPresenceOptions): Promise<Identifiers>;
