import { App } from '../models/app/app.js';
interface BuildOptions {
    app: App;
    skipDependenciesInstallation: boolean;
    apiKey?: string;
}
declare function build({ app, skipDependenciesInstallation, apiKey }: BuildOptions): Promise<void>;
export default build;
