import { ExtensionTypes } from '../../../constants.js';
import { App } from '../../../models/app/app.js';
import { dependency } from '@shopify/cli-kit';
import { Command } from '@oclif/core';
export default class AppScaffoldExtension extends Command {
    static description: string;
    static examples: string[];
    static flags: {
        type: import("@oclif/core/lib/interfaces/parser.js").OptionFlag<string | undefined>;
        name: import("@oclif/core/lib/interfaces/parser.js").OptionFlag<string | undefined>;
        'clone-url': import("@oclif/core/lib/interfaces/parser.js").OptionFlag<string | undefined>;
        template: import("@oclif/core/lib/interfaces/parser.js").OptionFlag<string | undefined>;
        path: import("@oclif/core/lib/interfaces/parser.js").OptionFlag<string | undefined>;
        verbose: import("@oclif/core/lib/interfaces/parser.js").BooleanFlag<boolean>;
    };
    static args: {
        name: string;
    }[];
    run(): Promise<void>;
    validateExtensionType(type: string | undefined): Promise<void>;
    /**
     * If the type passed as flag is not valid because it has already been scaffolded
     * and we don't allow multiple extensions of that type, throw an error
     * @param app {App} current App
     * @param type {string} extension type
     */
    validateExtensionTypeLimit(app: App, type: string | undefined): void;
    validateExtensionFlavor(type: string | undefined, flavor: string | undefined): void;
    /**
     * Some extension types like `theme` and `product_subscription` are limited to one per app
     * Use this method to retrieve a list of the limited types that have already been scaffolded
     *
     * @param app {App} current App
     * @returns {string[]} list of extensions that are limited by quantity and are already scaffolded
     */
    limitedExtensionsAlreadyScaffolded(app: App): string[];
    formatSuccessfulRunMessage(extensionType: ExtensionTypes, extensionDirectory: string, depndencyManager: dependency.DependencyManager): string;
}
